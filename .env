# Default Environment Configuration
# This file is used when no specific environment mode is set
# Development environment is the default

# Environment: dev, qa, uat, or prod
VITE_ENVIRONMENT=dev

# API Base URL (optional - will use environment-based default if not set)
VITE_API_BASE_URL=https://api-dev.datamatter.tech

# WebSocket Base URL (optional - will use environment-based default if not set)
VITE_WS_BASE_URL=wss://api-dev.datamatter.tech
