# Environment Configuration Example
# Copy this file to .env and adjust values as needed
# For environment-specific configs, create .env.development, .env.qa, .env.uat, .env.production

# Environment: dev, qa, uat, or prod
# Defaults to 'dev' if not specified
VITE_ENVIRONMENT=dev

# API Base URL
# If not set, will use environment-based default
VITE_API_BASE_URL=https://api-dev.datamatter.tech

# WebSocket Base URL
# If not set, will use environment-based default
VITE_WS_BASE_URL=wss://api-dev.datamatter.tech
